# Runtime deps for edo-daemon (root sidecar). Install into /opt/edo/venv —
# edo-daemon.service's ExecStart points directly at that venv's interpreter
# (modern Debian/Ubuntu block `pip install` into system Python via PEP 668,
# and a venv is invisible to a root-owned systemd unit unless ExecStart
# names its interpreter explicitly, which it does).
docker>=7.0,<8.0
